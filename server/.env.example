### Server environment example for development / deployment
### Note: Do NOT check secrets into source control. Use platform secret manager for production.

# JWT secret (at least 256-bit random string recommended)
JWT_SECRET=supersecretreplaceinproduction

# Encryption key used for aes-256-cbc in Session Notes and Secure Messaging
# Must be at least 32 bytes; we'll derive/trim to 32 bytes internally
ENCRYPTION_KEY=dev_encryption_key_replace_in_production

# MongoDB connection string (Mongo Atlas or other)
MONGODB_URI=mongodb://username:password@host:27017/stitch_therapy

# CORS / client origin (public app)
CLIENT_ORIGIN=https://your-frontend.example.com
CORS_ORIGIN=https://your-frontend.example.com

# Optional: SMTP settings for transactional emails
# If not set, email sending is skipped
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=XX
SMTP_PASS=XX
SMTP_FROM=no-reply@example.com

# Admin/therapist seeded account passwords (optional for initial deploy)
ADMIN_PW=ChangeMeAdminPassword
THERAPIST_PW=ChangeMeTherapistPassword

# Platform name for use in email templates
PLATFORM_NAME=Happiness Therapy
# ============================================
# DATABASE CONFIGURATION
# ============================================
# For local development with MongoDB Compass:
# MONGODB_URI=mongodb://localhost:27017/happiness-therapy-app

# For production with MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/happiness-therapy-app?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://stevebranny254_db_user:RM52i5pISihAtEIB@cluster0.i35pgfl.mongodb.net/?appName=Cluster0

# ============================================
# JWT & SECURITY
# ============================================
# Generate with: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=4000
NODE_ENV=development

# ============================================
# CORS & FRONTEND
# ============================================
# Local development:
# CORS_ORIGIN=http://localhost:5173

# Production (Vercel):
# CORS_ORIGIN=https://happiness-therapy-app-frontend.vercel.app
CORS_ORIGIN=http://localhost:5173

# ============================================
# DEFAULT CREDENTIALS (Change in production!)
# ============================================
THERAPIST_EMAIL=hapiness@example.com
THERAPIST_PASSWORD=password123

ADMIN_EMAIL=mwaniki@example.com
ADMIN_PASSWORD=password123
